[project]
name = "openframe-printer-newbuild"
version = "2.0.0"
description = "OpenFrame M1 from-scratch modular printer design package"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["openframe_printer"]
